apply plugin: 'eclipse'
apply plugin: 'idea'

subprojects {
    apply plugin: 'java'

    tasks.withType(JavaCompile) {
        group = 'com.epam.ivanou4'
        version = '0.0.1-SNAPSHOT'
        sourceCompatibility = 1.8
    }

    repositories {
        mavenCentral()
    }
}
